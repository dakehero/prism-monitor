# v0.7.2 Live UI Review Attempt

Date: 2026-07-10

Branch: `codex/0.7-low-power-snapshot`

Commit under review: `2f4523c`

## Purpose

Validate the remaining v0.7.2 live UI/render gate for the Rules workflow after automated tests proved rule behavior across Processes, History, tray, and Toast snapshot outputs.

## Build Used

Local unpackaged Release build:

```text
src/PrismMonitor.App/bin/Release/net10.0-windows10.0.26100.0/win-arm64/PrismMonitor.App.exe
```

The app was launched twice. The first launch entered tray mode; the second activation opened the main window, matching the app lifetime behavior.

Observed process state:

```json
[
  {
    "Id": 51040,
    "MainWindowHandle": { "value": 0 },
    "Path": "C:\\Program Files\\WindowsApps\\dakehero.prism-monitor_0.6.1.0_arm64__hctw1b8wxfke0\\PrismMonitor.App.exe"
  },
  {
    "Id": 55452,
    "MainWindowHandle": { "value": 7475332 },
    "Path": "C:\\Users\\dake_\\.codex\\worktrees\\f856\\native-guard\\src\\prismmonitor.app\\bin\\release\\net10.0-windows10.0.26100.0\\win-arm64\\prismmonitor.app.exe"
  }
]
```

## Evidence Collected

Computer Use could enumerate the local Prism Monitor window:

```json
{
  "app": "process:C:\\Users\\dake_\\.codex\\worktrees\\f856\\native-guard\\src\\PrismMonitor.App\\bin\\Release\\net10.0-windows10.0.26100.0\\win-arm64\\PrismMonitor.App.exe",
  "id": 7475332,
  "title": "Prism Monitor"
}
```

Accessibility text confirmed the main window content and navigation were present:

```text
Processes
History
Rules
Compatibility-mode processes
2 active processes
```

## Blocker

The same Computer Use session could not complete the required visual render review:

- `get_window_state` returned a screenshot, but the image showed the Windows lock/background surface rather than the Prism Monitor WinUI window.
- `activate_window` failed with:

```text
failed to activate captured window
```

Because activation failed, interactive navigation to the Rules page, tray menu, and Toast activation path could not be performed reliably through this automation channel.

## Result

Partially verified:

- The local Release app starts.
- A main Prism Monitor window is created with a non-zero window handle.
- UI Automation can read the main window navigation and Processes page text.

Not verified in this attempt:

- Visual alignment and spacing on the Rules page.
- The `Change target` flyout rendering.
- Tray right-click menu contents.
- Toast notification rendering and click activation.
- Live rule effects after interacting with Rules, tray, or Toast UI.

## Follow-Up

Use one of these paths before tagging v0.7:

- Run Visual Studio XAML Live Preview plus Hot Reload locally and capture Processes, History, Rules, Settings, tray menu, and Toast activation surfaces.
- Install a current MSIX test package and perform manual review on an unlocked desktop session.
- Add a dedicated WinUI UI smoke path only if repeated manual review remains too slow or unreliable.

## Review Package Prepared

A current branch MSIX review package was generated for manual review without installing it:

```text
C:\Users\dake_\.codex\worktrees\f856\native-guard\artifacts\local-msix\PrismMonitor-v0.7.2-review-20260710-043358\release\PrismMonitor-v0.7.2-review-win-arm64-msix.zip
```

Package payload check:

- MSIX generation completed for `win-arm64` Native AOT.
- A local test root certificate and `Install.ps1` were bundled beside the MSIX.
- The MSIX zip contents were inspected and no `onnx`, `DirectML`, or `MachineLearning` payload entries were found.

Important install note:

- The package identity still matches the development identity `dakehero.prism-monitor`.
- `Install.ps1` may replace the currently installed local package if Windows cannot update it in place.
- Use this package only for manual review after confirming it is acceptable to replace the current local Prism Monitor installation.
